{
  "name": "ore-phina",
  "version": "1.0.0",
  "description": "ore ore parcel + phina.js sample",
  "main": "src/index.html",
  "scripts": {
    "clear": "rm -rf ./dist && rm -rf ./.cache && rm -rf build",
    "dev": "npm run clear && parcel src/index.html",
    "build": "npm run clear && parcel build src/index.html -d build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^1.9.7"
  },
  "dependencies": {
    "phina.js": "^0.2.3"
  }
}
